descriptor_version: "1.0"

vendor: "test.fixture.onboarding"
name: "vnf-2"
version: "2.2.2"

virtual_deployment_units:
  - id: "vdu01"
    resource_requirements:
      cpu:
        vcpus: 1
      memory:
        size: 1
        size_unit: "GB"
